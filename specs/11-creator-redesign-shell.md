# 11 — Creator redesign: design system + shell + USD

Status: LOCKED for implementation (approved plan).

## Goal

Adopt the b2bAd creator aesthetic and shell while keeping the **naano** brand name. Switch money display to USD.

## Deliverables

1. Cream/black design tokens in `frontend/src/index.css`
2. Left icon-rail `CreatorShell` + top bar (gear, wallet, EN, bell, avatar menu)
3. Reusable dark `DealLinkCard` component
4. Dark circular AI guide FAB (GuideBar)
5. USD money helpers (keep `*Cents` field names; alias old euro helper names)

## Out of scope

Schema changes, new pages content, onboarding (later steps).
