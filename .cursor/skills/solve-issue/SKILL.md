---
name: solve-issue
description: >-
  Legacy alias for /swe. Prefer skill `swe`. Use when the user runs
  /solve-issue so the collaborative SWE pipeline still starts.
disable-model-invocation: true
---

# /solve-issue → /swe

This skill is a **compatibility shim**. Follow `.cursor/skills/swe/SKILL.md`
exactly. Same stages, gates, and artifacts (`brief.md` + `plan.md`, not
ticket-first Linear flow).
