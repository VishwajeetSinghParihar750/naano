# Brief

## Goal

Rebuild **naano.com** as a live, deployable product for the 8x assignment (24h window, not enforced). Ship something working and good — better than the original if we choose to.

## Context

- Product to study: https://naano.com
- Assignment requires using the real product end-to-end (signup + all flows), screenshots, then rebuild.
- Capture setup already done (`CAPTURE-TEST.md`, `.agent-logs/` hooks). Commit logs interleaved with work.
- Hand-in: live public link + public repo (with `.agent-logs/`) + ≤5 min walkthrough (camera on).

## Acceptance criteria

- Live link opens for someone who is **not** signed in as us
- Public repository with `.agent-logs/` committed
- Walkthrough ≤5 min, camera on
- Judged on: speed (working product), product judgement (what first / what cut), UX/UI quality

## Constraints

- Do **not** implement until product recon + shared plan approval (`/swe` SHAPE gate)
- Capture must stay working; commit `.agent-logs/` as we go
- Human-led planning; agent implements after approve

## Out of scope

- Using all 24 hours
- Perfect 1:1 clone of every edge feature before a shippable core

## Notes

- Start by walking creator vs business (brand) sides of naano.com with screenshots under `recon/`
- Then shape MVP together before any build
