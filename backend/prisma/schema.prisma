generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

/// Throwaway model — proves migrations run. Replaced by domain models in S02.
model HealthCheck {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
}
